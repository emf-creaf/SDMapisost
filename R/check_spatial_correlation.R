check_patial_correlation <- function(x) {



  # Check.
  if (!is.list(x)) cli::cli_abort("Input 'x' must be a named list")

  # Species.
  species <- names(x)


  for (sp in species) {
    y <- x[[sp]]

  }




}

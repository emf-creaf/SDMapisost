---
title: "Descarga de un MDT"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Descarga de un MDT}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```


## Introducción





## Zona de estudio

Como en documentos anteriores, descargaremos los límites geográficos de la CCAA de Madrid para poder recortar y enmascarar el mapa CORINE. Igualmente lo reproyectamos a EPSG:25830, sistema en el que trabajaremos durante todo el proyecto.

```{r Madrid}
# install.packages("mapSpain", "sf")
limites_madrid <- mapSpain::esp_get_ccaa("Madrid", year = "2024",
                                        epsg = "4258", resolution = "01") |>
  sf::st_geometry() |>
  terra::vect() |>
  terra::project("EPSG:25830")
```


## Modelo Digital del Terreno

En la sección de los MDT del CNIG, escojo el ámbito de la Comunidad de Madrid para el MDT de 25 metros de resolución y descargo las diferentes hojas que conforman el MDT para la Comunidad de Madrid en el directorio de nuestra elección. Podemos escoger la resolución que deseemos ya que el script que viene a continuación no distingue entre nombres de ficheros. Tan solo hay que tener en cuenta que el directorio en el que hemos depositado los archivos ".tif" del CNIG no puede tener nada más que esos archivos.


EXPLICAR MEJOR CÓMO SE DESCARGA, EL SITIO WEB EXACTO, ETC.

Transformamos el polígono que delimita la provincia de Madrid para que tenga el mismo sistema de referencia que el MDT. Esta transformación es más fácil y rápida que transformar todo el MDT.

```{r MDT}
# Modificar la carpeta destino a conveniencia.
destino <- "C:/imidra/mdt"
lista_mdt <- list.files(path = destino, pattern = "MDT25")

# Lo cargamos todo como una collección..
coleccion <- terra::sprc(file.path(destino, lista_mdt))
mdt  <- terra::merge(coleccion)

# Reproyectamos el MDT y lo recortamos.
mdt <- terra::project(mdt,  "EPSG:25830") |>
  terra::crop(limites_madrid, snap = "out") |>
  terra::mask(limites_madrid)
terra::writeRaster(mdt, file.path(destino, "mdt_madrid.tif"), overwrite = TRUE)
```



